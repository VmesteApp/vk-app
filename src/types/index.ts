export interface ITag {
  id: number;
  name: string;
}

export interface IPulse {
  id: number;
  category: string;
  name: string;
  founder_id: number;
  description: string;
  short_description: string;
}
